#!/usr/bin/env bash
# Скрипт сборки Django-проекта для Render
# Выполняется ТОЛЬКО при деплое (не при каждом запуске)

set -o errexit  # Выход при ошибке
set -o pipefail # Выход при ошибке в пайпе
set -o nounset  # Выход при использовании необъявленных переменных

echo "=========================================="
echo "🚀 НАЧАЛО СБОРКИ DJANGO ПРОЕКТА"
echo "=========================================="
echo "Время: $(date)"
echo "Python: $(python --version)"
echo "Pip: $(pip --version)"
echo ""

# 1. ОБНОВЛЕНИЕ ПАКЕТОВ И УСТАНОВКА ЗАВИСИМОСТЕЙ
echo "1. 📦 Установка зависимостей Python..."
pip install --upgrade pip
pip install -r requirements.txt

# 2. СОЗДАНИЕ СТРУКТУРЫ ДИРЕКТОРИЙ
echo "2. 📁 Создание структуры директорий..."
mkdir -p logs media staticfiles
mkdir -p media/shop_items media/uploaded_images 2>/dev/null || true
mkdir -p staticfiles/{css,js,assets} 2>/dev/null || true

# 3. ПРОВЕРКА ПОДКЛЮЧЕНИЯ К БАЗЕ ДАННЫХ (Supabase)
echo "3. 🔍 Проверка подключения к Supabase..."
if python -c "
import os, django
os.environ.setdefault('DJANGO_SETTINGS_MODULE', 'TeddyTale.settings')
django.setup()
from django.db import connection
try:
    connection.ensure_connection()
    print('✅ Подключение к Supabase успешно')
except Exception as e:
    print(f'❌ Ошибка подключения к Supabase: {e}')
    exit(1)
"; then
    echo "✅ База данных доступна"
else
    echo "❌ Не удалось проверить подключение к БД"
    # Не выходим с ошибкой - возможно, миграции ещё не применены
fi

# 4. СОБОР СТАТИЧЕСКИХ ФАЙЛОВ
echo "4. 🎨 Сбор статических файлов..."
python manage.py collectstatic --noinput --clear

echo ""
echo "=========================================="
echo "✅ СБОРКА УСПЕШНО ЗАВЕРШЕНА"
echo "=========================================="